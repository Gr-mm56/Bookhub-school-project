@model WebMVC.Areas.Admin.Models.Order.OrderCreateEditViewModelWithOptions

@{
    ViewBag.Title = "Create Order";
    Layout = "_Layout";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="card">
                <div class="card-header">
                    <h3>Create New Order</h3>
                </div>
                <div class="card-body">
                    <form asp-action="Create" method="post" id="createOrderForm" novalidate>
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                        <div class="row">
                            @await Html.PartialAsync("_FormBasicFields", Model)
                        </div>

                        <div class="row">
                            @await Html.PartialAsync("_FormUser", Model)
                        </div>

                        @await Html.PartialAsync("_FormMultiselects", Model)

                        <div class="d-flex justify-content-between mt-4">
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left">Back to List</i>
                            </a>
                            <button type="submit" class="btn btn-primary" id="submitBtn">
                                <i class="bi bi-save">Create Order</i>
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    @{
        ViewBag.FormId = "createOrderForm";
        ViewBag.SubmitBtnId = "submitBtn";
    }
    @await Html.PartialAsync("_FormValidationScript")
}
